Algoritmo "Enquanto"
Var

   x, soma : inteiro

Inicio

      escreval("Digite um numero: ")
      leia(x)

      soma <- 0
      enquanto (x <> 0) faca
               soma <- soma + x
               escreval("Digite outro numero: ")
               leia(x)
               fimenquanto
               
               escreval("Soma: ", soma)

Fimalgoritmo